import java.util.EventObject;

public class TempCoolEvent extends EventObject {
	/**
	 * The event is created with the identity of the
	 * 
	 * @param source
	 *            the object that created the event
	 */
	public TempCoolEvent(Object source) {
		super(source);
	}

}